dependencies:
  pre:
    - echo y | android update sdk --no-ui --all --filter "tools,platform-tools,android-24,extra-android-m2repository,"

test:
  override:
    # Build the code
    - ./gradlew clean assemble --console=plain
    - ./gradlew clean assemble -Dpre-dex=false --console=plain
